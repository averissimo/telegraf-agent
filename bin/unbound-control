#!/bin/bash

# taken from https://stackoverflow.com/a/42955871/302485
docker exec -ti pi_unbound.1.$(docker service ps -f 'name=pi_unbound.1' pi_unbound -q --no-trunc | head -n1) /opt/unbound/sbin/unbound-control $@
